# MySQL Configuration

COCLA works with standard MySQL 8.0+. No special configuration required.

Default users created by `create_users.sql`:
- root/root - full access
- cocla/cocla - access to cocla database
- grafana/grafana - read access to cocla, full access to grafana

For custom MySQL configuration, refer to MySQL documentation.